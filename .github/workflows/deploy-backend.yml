# Deploy HANYA folder backend/ ke Azure App Service (frontend di Vercel, bukan di Azure)
# WORKING_DIRECTORY = ./backend  → setara "root directory backend" di Azure
#
# SETUP (sekali, via browser):
# 1. Azure Portal → Web App → Download publish profile
# 2. GitHub repo → Settings → Secrets → Actions:
#    - AZURE_WEBAPP_NAME        = nama web app (contoh: krs-api-xxx)
#    - AZURE_WEBAPP_PUBLISH_PROFILE = isi file publish profile (XML)
# 3. Azure → Configuration → Application settings:
#    DATABASE_URL, REDIS_URL, JWT_SECRET, CORS_ORIGIN, MODE=production
#
# Jalankan: push ke branch main
#
# --- ALTERNATIF: Azure Container Apps ---
# Uncomment job deploy-container-apps di bawah dan set secrets:
#   ACR_LOGIN_SERVER, ACR_USERNAME, ACR_PASSWORD
#   AZURE_CREDENTIALS (service principal JSON)
#   AZURE_CONTAINER_APP_NAME, AZURE_RESOURCE_GROUP
# Lihat DEPLOY.md §7

name: Deploy Backend (Azure App Service)

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  WORKING_DIRECTORY: ./backend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.WORKING_DIRECTORY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: |
          npm run build
          mkdir -p build
          cp -r dist build/
          cp -r migrations build/
          cp package.json package-lock.json build/
          cd build && npm ci --omit=dev

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: backend/build

  # ---------------------------------------------------------------------------
  # Container Apps path (nonaktif — hapus komentar jika pakai Docker + ACR)
  # ---------------------------------------------------------------------------
  # build-and-push-acr:
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Login to ACR
  #       uses: azure/docker-login@v2
  #       with:
  #         login-server: ${{ secrets.ACR_LOGIN_SERVER }}
  #         username: ${{ secrets.ACR_USERNAME }}
  #         password: ${{ secrets.ACR_PASSWORD }}
  #     - name: Build and push
  #       run: |
  #         docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/krs-api:${{ github.sha }} ./backend
  #         docker push ${{ secrets.ACR_LOGIN_SERVER }}/krs-api:${{ github.sha }}
  #     - name: Deploy to Container Apps
  #       uses: azure/login@v2
  #       with:
  #         creds: ${{ secrets.AZURE_CREDENTIALS }}
  #     - name: Update container app
  #       uses: azure/cli@v2
  #       with:
  #         inlineScript: |
  #           az containerapp update \
  #             --name ${{ secrets.AZURE_CONTAINER_APP_NAME }} \
  #             --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
  #             --image ${{ secrets.ACR_LOGIN_SERVER }}/krs-api:${{ github.sha }}
